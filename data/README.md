# Data directory

Place local CSV or Parquet files here when experimenting. Repository defaults use deterministic synthetic data, so no market dataset is required to run the demo.

Tracked data policy:

- Do not commit account exports.
- Do not commit private or licensed datasets.
- If external data is used, document provenance and keep large files out of Git.
- Generated files in this directory are ignored by Git except for this README.
