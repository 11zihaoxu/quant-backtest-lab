# Examples

Run the deterministic synthetic demo:

```bash
python -m quantlab demo --config configs/demo.yaml --output results/demo
```

Or execute the wrapper:

```bash
python examples/run_demo.py
```

The sample strategy is a teaching example and is unrelated to any private production strategy. All results are generated from synthetic data.
